<template>
  <div class="app">
    <main>
      <NuxtPage />
    </main>
  </div>
</template>

<style scoped>
.app {
  max-width: calc(var(--max-width-site) + var(--margin-site) * 2);
  padding: var(--margin-content) var(--margin-site);
  margin: 0 auto;
}
</style>
